---
description: 应用开发经验 hub；收工更新个人 skill，不要堆补丁
alwaysApply: true
---

# H2 × h2ops

动手前读个人 skill **`h2ops`**，再按改动面读 **一个**情况 skill：`app-dev-code` / `app-dev-ui` / `app-dev-network` / `app-dev-release` / `app-dev-sync`。本仓命令在 **`h2-schedule-ops`**。

真机没变先发布与网络，不要先改样式。收工把新不变量写进对应情况 skill（普适化），不要只在 `failures.md` 加一行个例。
